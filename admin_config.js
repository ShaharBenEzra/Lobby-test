/*
  🔐  הגדרות מצב ניהול (אדמין)
  ============================================================
  מצב הניהול מאפשר לוועד הבית לערוך לבד את "הודעות ועד בית" ואת
  "סטטוס בניין" ישירות מהדפדפן - והשינוי מתפרסם לכל המסכים.

  איך זה עובד:
    הוועד עורך  →  Netlify Function עושה commit לריפו  →  Netlify
    מפרסם מחדש  →  כל מסך מושך content/*.json כל 2 דקות ומתעדכן לבד.

  הכניסה: כפתור המנעול 🔒 בפינה למטה (או Ctrl+Shift+A).

  ⚠️  הסיסמה האמיתית *לא* נמצאת בקובץ הזה ולא בריפו. היא יושבת
      כמשתנה סביבה בהגדרות של Netlify, והבדיקה נעשית בצד השרת.
      ראו ADMIN_SETUP.md.
  ============================================================

  1) ADMIN_API - כתובת פונקציית הניהול.
     "/.netlify/functions/admin"  → מצב מלא: פרסום אמיתי לכל המסכים
     ""                           → מצב מקומי בלבד (ראו סעיף 3)

  2) CONTENT_URLS - הקבצים שכל מסך מושך כדי לדעת מה להציג.
     זה מקור האמת. קבצי notices_data.js / building_status_data.js
     נשארים רק כגיבוי לטעינה הראשונה ולמצב בלי אינטרנט.

  3) CONTENT_POLL_MINUTES - כל כמה דקות כל מסך בודק אם יש תוכן חדש.
     0 = בלי בדיקות אוטומטיות (רק בטעינת הדף).

  4) ADMIN_LOCAL_PASSWORD - סיסמה לשימוש **רק** כשאין פונקציה זמינה
     (פתיחת הקבצים מהמחשב, בדיקות מקומיות). במצב הזה העריכה נשמרת
     בדפדפן הנוכחי בלבד ולא מתפרסמת לאף מסך אחר - הפאנל אומר את זה
     במפורש. אין טעם לבחור פה סיסמה חזקה: היא גלויה בקוד ולא מגנה
     על שום דבר בפרודקשן.

  5) ADMIN_SESSION_HOURS - כמה שעות להישאר מחוברים אחרי כניסה מוצלחת.

  6) ADMIN_BUTTON_VISIBLE - האם להציג את כפתור המנעול על המסך.
     false → הכפתור מוסתר (מסך טלוויזיה "נקי"), והכניסה רק ב-Ctrl+Shift+A.
  ============================================================
*/
const ADMIN_API = "/.netlify/functions/admin";

const CONTENT_URLS = {
    notices: "content/notices.json",
    status: "content/building_status.json"
};

const CONTENT_POLL_MINUTES = 2;

const ADMIN_LOCAL_PASSWORD = "1234";
const ADMIN_SESSION_HOURS = 8;
const ADMIN_BUTTON_VISIBLE = true;
