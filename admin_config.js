/*
  🔐  הגדרות מצב ניהול (אדמין)
  ============================================================
  מצב הניהול מאפשר לוועד הבית לערוך לבד את "הודעות ועד בית" ואת
  "סטטוס בניין" ישירות מהדפדפן - והשינוי מתפרסם לכל המסכים.

  איך זה עובד:
    הוועד עורך  →  Netlify Function שומרת ל-Netlify Blobs (אחסון מובנה
    באתר)  →  כל מסך מושך את התוכן כל 2 דקות ומתעדכן לבד.

    השמירה נכנסת לתוקף **מיד** - אין commit, אין deploy מחדש, ואין
    צורך ב-token של GitHub.

  הכניסה: כפתור המנעול 🔒 בפינה למטה (או Ctrl+Shift+A).

  ⚠️  הסיסמה האמיתית *לא* נמצאת בקובץ הזה ולא בריפו. היא יושבת
      כמשתנה סביבה בהגדרות של Netlify (משתנה אחד: ADMIN_PASSWORD),
      והבדיקה נעשית בצד השרת. ראו ADMIN_SETUP.md.
  ============================================================

  1) ADMIN_API - כתובת פונקציית הניהול (כתיבה).
     "/.netlify/functions/admin"  → מצב מלא: פרסום אמיתי לכל המסכים
     ""                           → מצב מקומי בלבד (ראו סעיף 4)

  2) CONTENT_API - הכתובת שכל מסך מושך ממנה את התוכן. זה מקור האמת.

  3) CONTENT_FALLBACK_URLS - קבצים סטטיים בריפו, נקראים רק אם CONTENT_API
     לא זמין (למשל לפני הפרסום הראשון, או בהרצה מקומית). אם גם הם חסרים,
     נופלים לקבצי notices_data.js / building_status_data.js שבקוד.

  4) CONTENT_POLL_MINUTES - כל כמה דקות כל מסך בודק אם יש תוכן חדש.
     0 = בלי בדיקות אוטומטיות (רק בטעינת הדף).

  5) ADMIN_SESSION_HOURS - כמה שעות להישאר מחוברים אחרי כניסה מוצלחת.

  6) ADMIN_BUTTON_VISIBLE - האם להציג את כפתור המנעול על המסך.
     false → הכפתור מוסתר (מסך טלוויזיה "נקי"), והכניסה רק ב-Ctrl+Shift+A.
  ============================================================

  ⚠️  אין בקובץ הזה שום סיסמה, בכוונה.
      סיסמה בקוד של אתר סטטי גלויה לכל מבקר - היא לא מגנה על כלום, היא
      רק נראית כמו הגנה. בנוסף, Netlify סורקת את תוצרי ה-build ומפילה את
      ה-deploy אם ערך של משתנה סביבה מופיע בהם, כך שסיסמה בקוד הפכה כל
      התנגשות מקרית ל-deploy שבור.

      הסיסמה היחידה היא ADMIN_PASSWORD בהגדרות של Netlify, והיא נבדקת
      בצד השרת. בהרצה מקומית (localhost / file://) הפאנל נכנס ישר למצב
      מקומי בלי סיסמה - ממילא אין שם ממה להגן.
  ============================================================
*/
const ADMIN_API = "/.netlify/functions/admin";

const CONTENT_API = "/.netlify/functions/content";

const CONTENT_FALLBACK_URLS = {
    notices: "content/notices.json",
    status: "content/building_status.json"
};

const CONTENT_POLL_MINUTES = 2;

const ADMIN_SESSION_HOURS = 8;
const ADMIN_BUTTON_VISIBLE = true;
