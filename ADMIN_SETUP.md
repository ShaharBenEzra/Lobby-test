# הגדרת מצב ניהול (אדמין)

מצב הניהול מאפשר לוועד הבית לערוך את **הודעות ועד בית** ואת **סטטוס בניין**
מהדפדפן, והשינוי מופיע על כל המסכים.

```
הוועד עורך  →  Netlify Function  →  Netlify Blobs (אחסון מובנה באתר)
                (בודקת סיסמה)              ↓
        כל מסך מושך את התוכן כל 2 דקות  →  מתעדכן לבד, בלי רפרש
```

**למה צריך פונקציה ולא פשוט לשמור מהדפדפן:** אתר סטטי לא יכול להחזיק סוד,
ולא יכול לשמור נתונים שכל המסכים יראו. הפונקציה רצה בצד השרת של Netlify:
היא המקום היחיד שמכיר את הסיסמה, והיא היחידה שכותבת לאחסון.

**למה Netlify Blobs:** זה אחסון מפתח-ערך מובנה באתר. אין חשבון נוסף, אין
token, ואין המתנה ל-deploy — שמירה נכנסת לתוקף מיד.

---

## מה צריך לעשות פעם אחת

### משתנה סביבה אחד

בממשק של Netlify: **Project configuration → Environment variables → Add a variable**

| שדה | ערך |
|---|---|
| Key | `ADMIN_PASSWORD` |
| Contains secret values | **לא לסמן** – הסימון הזה דורש חשבון בתשלום |
| Scopes | `All scopes` (ברירת המחדל) |
| Values | `Same value for all deploy contexts` |
| Value | סיסמה ארוכה לבחירתכם – **זו כל ההגנה** |

זה הכול. אין token, אין הרשאות, אין עוד משתנים.

### ואז Trigger deploy

**Deploys → Trigger deploy → Deploy project**

משתני סביבה נכנסים לתוקף רק ב-deploy הבא – גם עכשיו, וגם אחרי כל שינוי
עתידי בהם. זו הטעות שמפילה את רוב האנשים.

### לבדוק

לפתוח את האתר → כפתור המנעול 🔒 → **"בדיקת חיבור והגדרות"**.

אמור להופיע בירוק: *"הפונקציה פרוסה ו-ADMIN_PASSWORD מוגדר. אפשר להתחבר."*
אחר כך להזין את הסיסמה, לערוך משהו וללחוץ **פרסום** – השינוי אמור להופיע
על כל מסך פתוח תוך עד שתי דקות.

---

## מה נשמר איפה

| מקום | תפקיד |
|---|---|
| **Netlify Blobs** (store `dashboard-content`) | **מקור האמת.** נכתב ע"י הפונקציה, נקרא ע"י כל מסך |
| `content/notices.json`, `content/building_status.json` | גיבוי – נקראים רק לפני הפרסום הראשון או בהרצה מקומית |
| `notices_data.js`, `building_status_data.js` | גיבוי אחרון – אם גם ה-JSON לא נגיש |

תוכן שנשמר ב-Blobs נשאר שם בין deploys – פרסום מחדש של האתר לא מוחק אותו.

| קובץ | תפקיד |
|---|---|
| `netlify/functions/admin.js` | בודקת סיסמה, כותבת ל-Blobs (POST) |
| `netlify/functions/content.js` | קוראת מ-Blobs לכל מסך (GET, ציבורי) |
| `netlify/lib/content_shape.mjs` | מבנה הנתונים וניקוי הקלט – משותף לשתיהן |
| `package.json` | תלות אחת: `@netlify/blobs`. Netlify מתקינה אותה לבד |
| `netlify.toml` | מיקום הפונקציות + ביטול cache |
| `admin_config.js` | הגדרות לקוח – **בלי סודות** |

---

## מצב מקומי

אם הפונקציות לא זמינות – פתיחת הקבצים מהמחשב, אין אינטרנט, או שהאתר עוד לא
פורסם – הפאנל עובר אוטומטית ל**מצב מקומי**:

* הסיסמה נבדקת מול `ADMIN_LOCAL_PASSWORD` שב-`admin_config.js`
* השמירה נשמרת בדפדפן הנוכחי בלבד ו**לא** מגיעה לשאר המסכים
* הפאנל מציג אזהרה אדומה, וכפתור השמירה נקרא "שמירה מקומית"

זה נועד לפיתוח ובדיקות. `ADMIN_LOCAL_PASSWORD` גלויה בקוד ולא מגנה על כלום
בפרודקשן – הסיסמה האמיתית היא `ADMIN_PASSWORD` שב-Netlify.

להרצה מקומית עם הפונקציות האמיתיות:

```bash
netlify dev
```

---

## מה כדאי לדעת

**הסיסמה נשמרת בדפדפן של הוועד** למשך `ADMIN_SESSION_HOURS` (ברירת מחדל 8),
כדי שלא צריך להקליד אותה בכל שמירה. "יציאה" מוחקת אותה מיד.

**ניחוש סיסמה בכוח**: הפונקציה מוסיפה השהיה של 400ms על כל סיסמה שגויה.
זה מאט תוקף אבל לא עוצר אותו – לכן הסיסמה צריכה להיות ארוכה.

**החלפת הסיסמה**: לשנות את `ADMIN_PASSWORD` ב-Netlify ולעשות Trigger deploy.
אין צורך לגעת בקוד. מי שמחובר כרגע יינעל בשמירה הבאה שלו.

**קצב העדכון**: `CONTENT_POLL_MINUTES` ב-`admin_config.js`. 2 דקות זה איזון
סביר; 0 מבטל בדיקות אוטומטיות ומשאיר עדכון רק בטעינת הדף.

**גיבוי ידני**: בפאנל יש "ייצוא לקובץ קוד" שמייצר את התוכן של
`notices_data.js` / `building_status_data.js`. שימושי אם רוצים שגם קבצי
הגיבוי שבריפו יהיו מעודכנים.
