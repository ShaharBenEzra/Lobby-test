# הגדרת מצב ניהול (אדמין)

מצב הניהול מאפשר לוועד הבית לערוך את **הודעות ועד בית** ואת **סטטוס בניין**
מהדפדפן, והשינוי מתפרסם לכל המסכים.

```
הוועד עורך  →  Netlify Function  →  commit לריפו  →  Netlify מפרסם מחדש
                (ה-token יושב פה)                         ↓
        כל מסך מושך content/*.json כל 2 דקות  →  מתעדכן לבד, בלי רפרש
```

**למה צריך פונקציה ולא פשוט לכתוב ל-GitHub מהדפדפן:** הדשבורד הוא אתר סטטי,
ולכן כל דבר שנמצא בקוד שלו גלוי לכל מי שפותח את הדף. אם ה-token של GitHub היה
בקוד, כל אחד היה יכול לקרוא אותו ולכתוב לריפו. הפונקציה רצה בצד השרת של
Netlify, והיא המקום היחיד שבו ה-token נמצא.

---

## מה צריך לעשות פעם אחת

### 1. ליצור GitHub Token

ב-GitHub: **Settings → Developer settings → Personal access tokens →
Fine-grained tokens → Generate new token**

| הגדרה | ערך |
|---|---|
| Repository access | **Only select repositories** → `Lobby-Dashboard-Tormus-8` |
| Permissions → Contents | **Read and write** |
| Expiration | לבחירתכם (בתפוגה צריך ליצור token חדש ולעדכן אותו ב-Netlify) |

שאר ההרשאות – להשאיר ללא גישה. ה-token הזה יכול לכתוב **רק** לריפו הזה.

> העתיקו את ה-token מיד – GitHub מציג אותו פעם אחת בלבד.
> אל תשמרו אותו בקוד, בריפו, או בצ׳אט.

### 2. להגדיר את משתני הסביבה ב-Netlify

בממשק של Netlify: **Site configuration → Environment variables → Add a variable**

| שם | ערך | הערות |
|---|---|---|
| `ADMIN_PASSWORD` | הסיסמה של הוועד | **בחרו סיסמה ארוכה** – זו כל ההגנה |
| `GITHUB_TOKEN` | ה-token מסעיף 1 | |
| `GITHUB_REPO` | `ShaharBenEzra/Lobby-test` | בפורמט `owner/repo` |
| `GITHUB_BRANCH` | `main` | |

> **כשעוברים מהבדיקה לפרודקשן** – כלומר מהריפו `Lobby-test` לריפו האמיתי
> `Lobby-Dashboard-Tormus-8` – צריך לשנות שני דברים:
> `GITHUB_REPO` ל-`ShaharBenEzra/Lobby-Dashboard-Tormus-8`, ו-`GITHUB_BRANCH`
> ל-**`master`** (הענף של הריפו הישן הוא `master`, לא `main`).
> ה-token מסעיף 1 צריך גישה לריפו שאליו אתם מצביעים.

### 3. לפרסם

לדחוף את הקבצים לריפו. Netlify יזהה את `netlify/functions/admin.js`
ויפרסם אותה אוטומטית ב-`/.netlify/functions/admin`.

### 4. לבדוק

לפתוח את האתר → כפתור המנעול 🔒 → להזין את `ADMIN_PASSWORD` →
לערוך משהו → **פרסום**. תוך דקה-שתיים השינוי אמור להופיע גם על הטלוויזיה,
בלי לגעת בה.

---

## מה נשמר איפה

| קובץ | תפקיד |
|---|---|
| `content/notices.json` | **מקור האמת** להודעות – הפונקציה מעדכנת אותו |
| `content/building_status.json` | **מקור האמת** לסטטוס הבניין |
| `notices_data.js` | גיבוי – נטען רק אם אין אינטרנט או שה-JSON חסר |
| `building_status_data.js` | גיבוי – אותו דבר |
| `netlify/functions/admin.js` | הפונקציה שבודקת סיסמה ועושה commit |
| `netlify.toml` | מיקום הפונקציות + ביטול cache על `content/` |
| `admin_config.js` | הגדרות לקוח (כתובת הפונקציה, קצב המשיכה) – **בלי סודות** |

כל commit של הפאנל נראה בהיסטוריית הגיט, כך שאפשר לראות מה שונה ומתי,
ולחזור אחורה אם צריך.

---

## מצב מקומי

אם הפונקציה לא זמינה – פתיחת הקבצים מהמחשב, אין אינטרנט, או שהאתר עוד לא
פורסם – הפאנל עובר אוטומטית ל**מצב מקומי**:

* הסיסמה נבדקת מול `ADMIN_LOCAL_PASSWORD` שב-`admin_config.js`
* השמירה נשמרת בדפדפן הנוכחי בלבד ו**לא** מתפרסמת לשאר המסכים
* הפאנל מציג על זה אזהרה אדומה, וכפתור השמירה נקרא "שמירה מקומית"

זה נועד לפיתוח ובדיקות. `ADMIN_LOCAL_PASSWORD` גלויה בקוד ולא מגנה על כלום
בפרודקשן – הסיסמה האמיתית היא `ADMIN_PASSWORD` שב-Netlify.

---

## מה כדאי לדעת

**הסיסמה נשמרת בדפדפן של הוועד** למשך `ADMIN_SESSION_HOURS` (ברירת מחדל 8),
כדי שלא צריך להקליד אותה בכל שמירה. לחיצה על "יציאה" מוחקת אותה מיד.

**ניחוש סיסמה בכוח**: הפונקציה מוסיפה השהיה של 400ms על כל סיסמה שגויה.
זה מאט תוקף אבל לא עוצר אותו – לכן חשוב שהסיסמה תהיה ארוכה. אם תרצו הגנה
חזקה יותר, אפשר להוסיף חסימה לפי IP או Netlify Identity.

**החלפת הסיסמה**: לשנות את `ADMIN_PASSWORD` ב-Netlify. אין צורך לגעת בקוד.
משתמשים שמחוברים כרגע יינעלו בשמירה הבאה שלהם.

**החלפת ה-token** (בתפוגה): ליצור חדש לפי סעיף 1 ולעדכן את `GITHUB_TOKEN`
ב-Netlify. שאר ההגדרות נשארות.

**קצב העדכון**: `CONTENT_POLL_MINUTES` ב-`admin_config.js` קובע כל כמה דקות
כל מסך בודק אם יש תוכן חדש. 2 דקות זה איזון סביר; 0 מבטל בדיקות אוטומטיות.
